This is a simple React frontend for the URL shortener.

Quick start:

1. cd frontend
2. npm install
3. npm run dev

Environment:
- VITE_API_BASE_URL (default: http://localhost:8080/api/v1)

This frontend provides Signup, Login, Create Short URL, List URLs, and Analytics views.
